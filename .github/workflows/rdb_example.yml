name: Example

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  rdb-example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: recursive

      - uses: cachix/install-nix-action@v15
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Run test
        run: nix develop -c 'run-example-test' rdb

      - name: Deploy test
        run: nix develop -c 'cli-build-test' rdb

      - name: Client test
        run: nix develop -c 'run-client-test' rdb
