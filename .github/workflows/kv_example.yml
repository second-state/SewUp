name: Example

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  kv-example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: recursive

      - uses: cachix/install-nix-action@v13
        with:
          nix_path: nixpkgs=channel:nixos-21.05-small

      - name: Run test
        run: nix-shell --run 'run-example-test kv'

      - name: Deploy test
        run: nix-shell --run 'cli-test kv'

      - name: Client test
        run: nix-shell --run 'run-client-test kv'
